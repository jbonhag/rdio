#!/bin/bash

URL=http://sleepy-reaches-5806.herokuapp.com

play() {
  echo "play"
  echo "$1"
  curl $URL --data "command=play&query=$1"
}

stop() {
  echo "stop"
  curl $URL --data "command=stop";
}

case $1 in
  "play") play "$2"
  ;;
  "stop") stop
  ;;
esac
